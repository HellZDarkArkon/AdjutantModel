#include "MessageBus.h"

void MessageBus::Send(const Message& msg)
{
	queue.push_back(msg); // Add message to the queue
}

std::vector<Message> MessageBus::Fetch()
{
	auto out = queue; // Copy current queue to output
	queue.clear(); // Clear the queue after fetching
	return out; // Return the fetched messages
}
